"use client";

import { motion } from "framer-motion";
import Header from "@/components/Header";
import Hero from "@/components/Hero";
import ContactForm from "@/components/ContactForm";

const fadeUp = {
  initial: { opacity: 0, y: 24 },
  whileInView: { opacity: 1, y: 0 },
  viewport: { once: true, margin: "-80px" },
  transition: { duration: 0.55, ease: "easeOut" },
};

const capabilities = [
  { num: "01", title: "Software Development", desc: "Custom enterprise software designed, built, and maintained for secure, scalable operations." },
  { num: "02", title: "Artificial Intelligence Solutions", desc: "AI models and intelligent automation that improve decisions, productivity, and service outcomes." },
  { num: "03", title: "IT Consulting", desc: "Strategic IT advisory aligning technology architecture, governance, and execution with business goals." },
  { num: "04", title: "Cloud Services", desc: "Cloud migration, modernization, and managed operations across hybrid and multi-cloud environments." },
  { num: "05", title: "Digital Platforms", desc: "End-to-end digital platforms that unify customer engagement, workflows, and service delivery." },
  { num: "06", title: "Cybersecurity", desc: "Security architecture, risk controls, and continuous defense to protect critical systems and data." },
  { num: "07", title: "Data Analytics", desc: "Data engineering and analytics that convert enterprise data into actionable performance intelligence." },
  { num: "08", title: "Systems Integration", desc: "Integration of enterprise applications, infrastructure, and APIs for seamless cross-system operations." },
  { num: "09", title: "Research & Development", desc: "Applied R&D programs that prototype and validate emerging technologies for institutional use." },
];

const whyItems = [
  { title: "Integrated capability, one architecture", desc: "AI, software, cloud, cybersecurity, data, and integration are aligned within a single delivery frame so strategy and execution remain coherent." },
  { title: "Enterprise-minded execution", desc: "Programs are delivered with structured governance, clear accountability, and operational discipline suited to complex institutional environments." },
  { title: "Secure and governed delivery", desc: "Security, privacy, and responsible controls are embedded into delivery decisions from design through deployment and ongoing operations." },
  { title: "Long-horizon capability building", desc: "Architectures are shaped for durability and adaptability, helping organizations build digital capability that continues to serve future priorities." },
];

const framework = [
  { num: "01", title: "Consult", subtitle: "Engagement & strategy alignment", desc: "Define business priorities, map constraints, and establish the transformation roadmap with executive and operational stakeholders." },
  { num: "02", title: "Design & Build", subtitle: "Software, platforms & AI systems", desc: "Architect and deliver enterprise software, digital platforms, and AI capabilities tailored to operating models and measurable outcomes." },
  { num: "03", title: "Integrate", subtitle: "Systems, data & workflow connection", desc: "Connect legacy and modern environments, unify data flows, and align automation with cross-functional business processes." },
  { num: "04", title: "Secure & Govern", subtitle: "Risk, compliance & control posture", desc: "Embed cybersecurity, privacy, model governance, and policy controls so solutions remain trusted, auditable, and resilient." },
  { num: "05", title: "Operate & Evolve", subtitle: "Managed capability & continuous improvement", desc: "Provide enablement, monitoring, optimization, and roadmap evolution so enterprise capability compounds over time." },
];

const platforms = [
  { num: "01 / Premium Direction", title: "Immersive Visualization", desc: "Real-time 3D interface environments that convert complex enterprise data into spatially clear, decision-ready visual systems." },
  { num: "02 / Operations Layer", title: "Digital Operations Systems", desc: "Integrated software command layers for workflow coordination, process governance, and institutional execution continuity." },
  { num: "03 / Modeling Stack", title: "Simulation Products", desc: "Scenario simulation engines for testing operational, infrastructure, and risk outcomes before production-scale deployment." },
  { num: "04 / Assurance Fabric", title: "Secure Enterprise Platforms", desc: "Hardened cloud and application foundations with embedded identity, compliance, and resilience controls for critical systems." },
];

const governance = [
  { title: "Security", desc: "Secure engineering standards, cloud controls, and continuous monitoring protect enterprise software, infrastructure, and digital services in production." },
  { title: "Oversight", desc: "Architecture reviews, change governance, and traceable approvals keep delivery decisions accountable across software, data, and platform programs." },
  { title: "Privacy", desc: "Data classification, access segmentation, and auditable handling practices preserve confidentiality across client records, analytics workflows, and shared environments." },
  { title: "Resilient Operations", desc: "Staged releases, rollback readiness, and observability-led operations sustain continuity for cloud workloads, enterprise platforms, and integrated digital services." },
];

const faqs = [
  { q: "What kinds of advanced 3D software does Austonix build?", a: "We develop custom 3D platforms such as digital twins, simulation and training environments, interactive configurators, engineering visualization tools, and real-time spatial analytics systems for enterprise operations." },
  { q: "Are 3D solutions pre-built products or tailored to each organization?", a: "Every engagement is architecture-led and custom. We may accelerate delivery with proven components, but data models, interaction logic, integrations, and governance controls are designed around your specific technical and business context." },
  { q: "How do you handle security, deployment, and operational reliability?", a: "Security and resilience are built in from day one. We implement role-based access, audit trails, secure pipelines, environment hardening, and staged deployment practices across cloud, on-prem, or hybrid infrastructure." },
  { q: "Can your 3D software integrate with our existing enterprise systems?", a: "Yes. We integrate with ERPs, GIS stacks, BIM/CAD sources, IoT streams, document systems, identity providers, and reporting platforms through secure APIs and middleware aligned with your existing architecture." },
  { q: "How does a typical custom 3D software engagement begin?", a: "We start with a structured discovery session, define use cases and success criteria, assess technical constraints, and produce a phased delivery roadmap with scope, governance checkpoints, and implementation milestones." },
];

export default function HomePage() {
  return (
    <>
      <Header />
      <main>
        <Hero />

        <section id="company" className="py-24 border-t border-border/60">
          <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <motion.div {...fadeUp} className="max-w-3xl">
              <span className="text-xs font-semibold tracking-widest text-gold uppercase">Institutional Company Overview</span>
              <h2 className="mt-3 font-display text-3xl sm:text-4xl font-bold tracking-tight text-zinc-50">
                Building the infrastructure behind intelligent enterprises
              </h2>
              <p className="mt-5 text-lg text-muted leading-relaxed">
                AUSTONIX GOLDEN HEIRS LTD is a Nigerian technology company and technology holding company. 
                We design, build, and integrate enterprise software, AI systems, cloud capability, cybersecurity architecture, 
                data infrastructure, digital platforms, and research-driven digital solutions for institutions that require 
                reliable long-term technology capacity.
              </p>
            </motion.div>
          </div>
        </section>

        <section id="capabilities" className="py-24 bg-surface/50">
          <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <motion.div {...fadeUp} className="max-w-3xl mb-14">
              <span className="text-xs font-semibold tracking-widest text-gold uppercase">Principal Business Activities</span>
              <h2 className="mt-3 font-display text-3xl sm:text-4xl font-bold tracking-tight text-zinc-50">
                Core capabilities powering intelligent enterprise operations
              </h2>
              <p className="mt-4 text-muted">
                Austonix delivers integrated software, AI, consulting, cloud, cybersecurity, data, and ICT execution services for institutional transformation.
              </p>
            </motion.div>

            <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-5">
              {capabilities.map((item, i) => (
                <motion.article
                  key={item.num}
                  initial={{ opacity: 0, y: 20 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ duration: 0.4, delay: i * 0.05 }}
                  className="group rounded-xl border border-border bg-card p-6 transition hover:border-gold/40 hover:-translate-y-1"
                >
                  <span className="text-sm font-display font-bold text-gold/80">{item.num}</span>
                  <h3 className="mt-3 text-lg font-semibold text-zinc-50 group-hover:text-gold transition-colors">{item.title}</h3>
                  <p className="mt-2 text-sm text-muted leading-relaxed">{item.desc}</p>
                </motion.article>
              ))}
            </div>
          </div>
        </section>

        <section className="py-24">
          <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <motion.div {...fadeUp} className="max-w-3xl mb-14">
              <span className="text-xs font-semibold tracking-widest text-gold uppercase">Why Organizations Engage Austonix</span>
              <h2 className="mt-3 font-display text-3xl sm:text-4xl font-bold tracking-tight text-zinc-50">
                A disciplined technology partner for integrated, secure, and future-ready enterprise transformation
              </h2>
            </motion.div>

            <div className="grid sm:grid-cols-2 gap-6">
              {whyItems.map((item, i) => (
                <motion.div
                  key={item.title}
                  initial={{ opacity: 0, y: 20 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ duration: 0.4, delay: i * 0.08 }}
                  className="rounded-xl border border-border bg-card/50 p-6"
                >
                  <h3 className="text-lg font-semibold text-gold mb-2">{item.title}</h3>
                  <p className="text-sm text-muted leading-relaxed">{item.desc}</p>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        <section id="framework" className="py-24 bg-surface/50">
          <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <motion.div {...fadeUp} className="max-w-3xl mb-14">
              <span className="text-xs font-semibold tracking-widest text-gold uppercase">Enterprise Capability Framework</span>
              <h2 className="mt-3 font-display text-3xl sm:text-4xl font-bold tracking-tight text-zinc-50">
                One delivery model from strategy to resilient operations
              </h2>
              <p className="mt-4 text-muted">
                Austonix connects consulting, software and AI build, systems integration, security governance, and long-term operational enablement so organizations can engage one institutional partner across the full transformation lifecycle.
              </p>
            </motion.div>

            <div className="space-y-4">
              {framework.map((step, i) => (
                <motion.div
                  key={step.num}
                  initial={{ opacity: 0, x: -16 }}
                  whileInView={{ opacity: 1, x: 0 }}
                  viewport={{ once: true }}
                  transition={{ duration: 0.4, delay: i * 0.07 }}
                  className="flex flex-col sm:flex-row gap-5 rounded-xl border border-border bg-card p-6 sm:p-7"
                >
                  <span className="font-display text-2xl font-bold text-gold shrink-0">{step.num}</span>
                  <div>
                    <h3 className="text-lg font-semibold text-zinc-50">{step.title}</h3>
                    <p className="text-sm text-gold mt-0.5 mb-2">{step.subtitle}</p>
                    <p className="text-sm text-muted leading-relaxed">{step.desc}</p>
                  </div>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        <section id="platforms" className="py-24">
          <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <motion.div {...fadeUp} className="max-w-3xl mb-14">
              <span className="text-xs font-semibold tracking-widest text-gold uppercase">Product Platforms</span>
              <h2 className="mt-3 font-display text-3xl sm:text-4xl font-bold tracking-tight text-zinc-50">
                Premium software pillars for immersive and enterprise-grade deployment
              </h2>
              <p className="mt-4 text-muted">
                Four platform directions shape immersive product delivery across visualization, operations, simulation, and secure enterprise architecture.
              </p>
            </motion.div>

            <div className="grid sm:grid-cols-2 gap-5">
              {platforms.map((p, i) => (
                <motion.article
                  key={p.title}
                  initial={{ opacity: 0, y: 20 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ duration: 0.4, delay: i * 0.08 }}
                  className="rounded-xl border border-border bg-card p-7 hover:border-gold/40 transition"
                >
                  <span className="text-xs font-semibold tracking-wider text-gold uppercase">{p.num}</span>
                  <h3 className="mt-3 text-xl font-semibold text-zinc-50">{p.title}</h3>
                  <p className="mt-3 text-sm text-muted leading-relaxed">{p.desc}</p>
                </motion.article>
              ))}
            </div>
          </div>
        </section>

        <section id="governance" className="py-24 bg-surface/50">
          <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <motion.div {...fadeUp} className="max-w-3xl mb-14">
              <span className="text-xs font-semibold tracking-widest text-gold uppercase">Institutional Governance Posture</span>
              <h2 className="mt-3 font-display text-3xl sm:text-4xl font-bold tracking-tight text-zinc-50">
                Enterprise technology delivery requires disciplined governance at every layer
              </h2>
              <p className="mt-4 text-muted">
                Security, accountability, privacy, and operational resilience are embedded across software systems, cloud platforms, data pipelines, and digital operations from architecture through live service delivery.
              </p>
            </motion.div>

            <div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-5">
              {governance.map((g, i) => (
                <motion.div
                  key={g.title}
                  initial={{ opacity: 0, y: 16 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ duration: 0.4, delay: i * 0.07 }}
                  className="rounded-xl border border-border bg-card p-6"
                >
                  <h3 className="text-base font-semibold text-gold mb-2">{g.title}</h3>
                  <p className="text-sm text-muted leading-relaxed">{g.desc}</p>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        <section className="py-24">
          <div className="mx-auto max-w-3xl px-4 sm:px-6 lg:px-8">
            <motion.div {...fadeUp} className="mb-12">
              <span className="text-xs font-semibold tracking-widest text-gold uppercase">3D Software FAQ</span>
              <h2 className="mt-3 font-display text-3xl sm:text-4xl font-bold tracking-tight text-zinc-50">
                Questions enterprise teams ask before commissioning custom 3D software
              </h2>
            </motion.div>

            <div className="space-y-3">
              {faqs.map((faq, i) => (
                <motion.details
                  key={i}
                  initial={{ opacity: 0, y: 12 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ duration: 0.35, delay: i * 0.05 }}
                  className="group rounded-xl border border-border bg-card overflow-hidden"
                >
                  <summary className="flex cursor-pointer items-center justify-between gap-4 px-6 py-4 text-sm font-semibold text-zinc-100 list-none">
                    {faq.q}
                    <span className="text-gold text-lg shrink-0 group-open:hidden">+</span>
                    <span className="text-gold text-lg shrink-0 hidden group-open:inline">−</span>
                  </summary>
                  <p className="px-6 pb-5 text-sm text-muted leading-relaxed">{faq.a}</p>
                </motion.details>
              ))}
            </div>
          </div>
        </section>

        <section id="contact" className="py-24 bg-surface/50 border-t border-border/60">
          <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <motion.div {...fadeUp} className="max-w-3xl mb-12">
              <span className="text-xs font-semibold tracking-widest text-gold uppercase">Corporate Engagement</span>
              <h2 className="mt-3 font-display text-3xl sm:text-4xl font-bold tracking-tight text-zinc-50">
                Start a business or enterprise conversation with Austonix
              </h2>
              <p className="mt-4 text-muted">
                Tell us where your organization needs progress across software, AI, consulting, cloud, cybersecurity, data, systems integration, digital platforms, research, or broader digital capability strategy.
              </p>
            </motion.div>

            <div className="grid lg:grid-cols-2 gap-12 items-start">
              <div>
                <h3 className="text-sm font-semibold text-gold mb-2">Direct enterprise email</h3>
                <a href="mailto:info@austonix.com" className="text-lg text-zinc-100 hover:text-gold transition">
                  info@austonix.com
                </a>

                <h3 className="text-sm font-semibold text-gold mt-8 mb-3">Engagement areas</h3>
                <ul className="space-y-2 text-sm text-muted">
                  <li className="flex gap-2"><span className="text-gold">–</span> Enterprise software modernization and custom application delivery</li>
                  <li className="flex gap-2"><span className="text-gold">–</span> AI, analytics, and automation programs for institutional operations</li>
                  <li className="flex gap-2"><span className="text-gold">–</span> Cloud architecture, cybersecurity posture, and resilience planning</li>
                  <li className="flex gap-2"><span className="text-gold">–</span> Systems integration, digital platforms, and connected infrastructure</li>
                  <li className="flex gap-2"><span className="text-gold">–</span> Consulting, research partnerships, and strategic technology roadmap design</li>
                </ul>
              </div>

              <ContactForm />
            </div>
          </div>
        </section>
      </main>

      <footer className="border-t border-border py-12">
        <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 text-center">
          <div className="flex items-center justify-center gap-3 mb-3">
            <span className="flex h-8 w-8 items-center justify-center rounded-md bg-gradient-to-br from-gold to-gold-dark font-display text-sm font-bold text-background">
              A
            </span>
            <span className="font-semibold text-zinc-100">Austonix Golden Heirs Ltd</span>
          </div>
          <p className="text-sm text-muted mb-4">Intelligent technology. Enterprise infrastructure. Digital capability.</p>
          <p className="text-xs text-muted/70">© {new Date().getFullYear()} Austonix Golden Heirs Ltd. All rights reserved.</p>
        </div>
      </footer>
    </>
  );
}